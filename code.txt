//Shop Interface System
#include <iostream>
#include <string>
#include <stdlib.h>
#include <fstream>
#include<string>
using namespace std;

class Shop {
protected:
    int price, quantity;
    string item_code, item_name, brand_name;
    fstream view_list, put_item;

public:
    void home_page(int&);
	void item_list();
};

void Shop::home_page(int& opt1) {
    cout << "------------------SHOP INTERFACE SYSTEM------------------" << endl;
    cout << "1. Admin" << endl;
    cout << "2. Customer" << endl << endl;
    cout << "Choose an option: ";
    cin >> opt1;

    system("CLS");
}

void Shop:: item_list() {
        view_list.open("Items.txt", ios::in);
        cout << "Item_code\t Item_name\t Brand_name\t Price\t Quantity" << endl;
        if (view_list.is_open()) {
            while (!view_list.fail()) {         
                view_list >> item_code >> item_name >> brand_name >> price >> quantity;
                cout << item_code << "\t" << item_name << "\t" << brand_name << "\t" << price << "\t" << quantity << endl;
            }
            view_list.close();
        }
        else {
            cout << "FILE NOT FOUND" << endl;
        }
}

class Admin :public Shop {
private:
    string ynopt;
public:
    void admin_option(int&);
    void add_item();
};

void Admin::admin_option(int& opt2) {
    cout << "------------------SHOP INTERFACE SYSTEM------------------" << endl;
    cout << "1. View Item List" << endl;
    cout << "2. Add Items" << endl;
    cout << "3. Remove Items" << endl;
    cout << "4. Edit Items" << endl;
    cout << "5. View Bills" << endl;
    cout << "6. Go to home screen" << endl << endl;
    cout << "Choose an option: ";
    cin >> opt2;

    system("CLS");

}

void Admin:: add_item() {
    put_item.open("Items.txt", ios::out | ios_base::app);
    Again:
    if (put_item.is_open()) {
        cout << "Enter Item code: ";
        cin >> item_code;
        cout << "Enter Item name: ";
        cin >> item_name;
        cout << "Enter Brand name: ";
        cin >> brand_name;
        cout << "Enter Price of item: ";
        cin >> price;
        cout << "Enter Quantity of item: ";
        cin >> quantity;

        put_item << item_code << " " << item_name << " " << brand_name << " " << price << " " << quantity<<" ";
        cout << "Do you want to add more? (y/n)";
        cin >> ynopt;
        if (ynopt == "y") {
            goto Again;
        }
        put_item.close();
    }
    else {
        cout << "FILE NOT FOUND" << endl;
    }
}

class Customer :public Shop {
private:
    int buy[20], quant[20];
    int i=0;
public:
    void buy_item();
    //void calculate(int[], int[]);
};

void Customer::buy_item() {
    cout << "\nWhat do you want to buy?" << endl;
    while (buy[i-1] != 99) {
        cin >> buy[i];
        if (buy[i] == 0)
            break;
        cout << "Enter quantity: ";
        cin >> quant[i];
        i++;
        cout << "\nAnother item: " << endl;
    }
}

int main() {
    Shop shop;
    Admin admin;
    Customer customer;
    int option1,option2,i=0;
    int buy[20], quant[20];

    Start:
    shop.home_page(option1);
    
    switch (option1) {
    //admin
    case 1: {
        admin.admin_option(option2);
        switch (option2) {
        case 1: {
            admin.item_list();
            break;
        }
        case 2: {
            admin.add_item();
            break;
        }
        }
        break;
    }

    //customer
    case 2: {
        customer.item_list();
        customer.buy_item();


        //customer.calculate(buy[20], quant[20]);
        break;
    }
    //exit
    case 3: {
        return 0;
    }

    default: {
        cout << "<<<<<<<<<<<<<INVALID OPTION>>>>>>>>>>>>" << endl;
        goto Start;
    }
    
    }

	return 0;
}
